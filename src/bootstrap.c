#include <stdlib.h>

#include "core.h"

int main(int argc, char **argv) {

    printf("Simple Bootstrapper\n");
    printf("sizeof(object_type) %li\n", sizeof(object_type));

    printf(">\n");

    exit(0);
}
