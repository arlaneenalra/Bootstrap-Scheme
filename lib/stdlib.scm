; Define some rather basic operations in scheme

;; ; define cons in scheme
;; (define (cons x y)
;;   (define pair '( () . () ))
;;   (set-car! pair x)
;;   (set-cdr! pair y)
;;   pair)

; load the string code library
(load "lib/string.h")